#include<bits/stdc++.h>
using namespace std;

int main(){
    double arr [5];
    cin >> arr [0] >> arr [1] >> arr [2] >> arr [3];
    arr [3] = 17;
    // cout << "the array is " <<  arr [3];
    for(int i = 0; i < 5; i++){
        cout << arr [i] << " ";
    }
    return 0;
}

